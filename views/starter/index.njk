{% extends "first-layout.njk"%}

{% block header %}
    <header class="container-header">
        <div class="img">
            <a href="/"><img src="/assets/logo1.png" alt="Logo"></a>
        </div>
        <a href="/starter/about">Sobre</a>
        <a href="/starter/recipies">Receitas</a>
    </header>
{% endblock %}

{% block content %}

    <section class="container-section">
        <div class="section-content">
            <h1>As melhores receitas</h1>
            <p>
                Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.
            </p>
        </div>
        <img src="/assets/chef.png" alt="">
    </section>

    <main class="container-cards">
        <h2>Mais acessadas</h2>

        {# O CONTEUDO FOI DUPLICADO AQUI E NO DATA.JS PARA COM O OBJETIVO DE DEIXAR COMO NO PRIMEIRO FOODFY APENAS O PRIMEIRO JÁ E O SUFICIENTE #}
        <div class="cards">

            {% for item in items %}

                <div class="card">
                    <img src="{{item.image}}" alt="Imagem de {{item.title}}">
                    <div class="card-text">
                        <h3 class="card-title">{{item.title}}</h3>
                        <p class="card-author">{{item.author}}</p>
                    </div>
                </div>

            {% endfor %}
            
        </div>
    </main>
</div>

{% endblock %}